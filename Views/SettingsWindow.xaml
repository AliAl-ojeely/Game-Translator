<Window x:Class="GameTranslator.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="480" Width="650"
        Background="#1E1E1E" Foreground="#FFFFFF"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="270"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,15">
            <TextBlock Text="LM Studio Endpoint URL:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtEndpoint" Text="http://127.0.0.1:1234/v1/chat/completions" Background="#2D2D2D" Foreground="White" BorderBrush="#333333" Padding="4" Margin="0,0,0,10"/>

            <TextBlock Text="Model Name:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtModel" Text="aya-23-8b" Background="#2D2D2D" Foreground="White" BorderBrush="#333333" Padding="4"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Delay in seconds between requests" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtDelay" Width="100" HorizontalAlignment="Left" Margin="0,5" Background="#2D2D2D" Foreground="White" BorderBrush="#333333" Padding="2"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Max bytes per request" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtMaxBytes" Width="100" HorizontalAlignment="Left" Margin="0,5" Background="#2D2D2D" Foreground="White" BorderBrush="#333333" Padding="2"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Max characters per string (skip if longer)" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtMaxChars" Width="100" HorizontalAlignment="Left" Margin="0,5" Background="#2D2D2D" Foreground="White" BorderBrush="#333333" Padding="2"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Text="Prompt text (Use {0} for Source, {1} for Target):" VerticalAlignment="Top" Margin="0,15,0,5"/>
        <TextBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" x:Name="TxtPromptTemplate" Margin="0,35,0,10" 
                 TextWrapping="Wrap" AcceptsReturn="True" Background="#2D2D2D" Foreground="#FFFFFF" BorderBrush="#333333" Padding="5"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">

            <!-- OK Button -->
            <Button Content="OK" Width="80" Height="30" Margin="0,0,10,0" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnOk_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#005A9E"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0078D7"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Cancel Button -->
            <Button Content="Cancel" Width="80" Height="30" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnCancel_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#404040"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#555555"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

        </StackPanel>
    </Grid>
</Window>